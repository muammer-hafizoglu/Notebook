@model IEnumerable<String>
@{
    ViewData["Title"] = "Login";
}

@foreach (var item in Model)
{
    <a class="btn btn-primary login-button" asp-action="SignInPost" asp-route-provider="@item">@item</a>
}